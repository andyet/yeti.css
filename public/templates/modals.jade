section.modals
  h3 Modals

  .modal

  h4 Overlay
  p 
    | If you need to cover the content of the page to display the overlay (probably most cases) &ndash; you will need an 
    em .overlay 
    | div appended to body of the page. It will create an semi-transparent black overlay.